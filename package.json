{
  "name": "segfault-controller",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel controller + keepalive cron for segfault instance",
  "type": "commonjs",
  "dependencies": {
    "ssh2": "^1.15.0"
  }
}
